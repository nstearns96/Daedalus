0 1
start:
	if 1 goto found_num
	move r
	goto start
	found_num:
		move r
		if 0 goto concat
		goto found_num
		concat:
			write 1
			move r
			if 1 goto seek_end
			reject
			seek_end:
				move r
				if 0 goto zero_end
				goto seek_end
				zero_end:
					move l
					write 0
					accept